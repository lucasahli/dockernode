type Reminder {
    id: ID!
    title: String
    owner: User
    usersToRemind: [User]
    isCompleted: Boolean
    dateTimeToRemind: DateTime
}

extend type Query {
    reminders: [Reminder]
    remindersByOwner(ownerId: ID!): [Reminder!]
    reminder(id: ID!): Reminder
}

extend type Mutation {
    createReminder(title: String!, dateTimeToRemind: DateTime!): Reminder
    deleteReminder(id: ID!): Boolean!
}